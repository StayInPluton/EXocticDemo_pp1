const express = require('express');
const router = express.Router();

// Rotas relacionadas à tabela Usuários
router.get('/', (req, res) => {
  // Obter todos os usuários
  // ...
});

router.post('/', (req, res) => {
  // Criar um novo usuário
  // ...
});

// Resto das rotas relacionadas aos Usuários

module.exports = router;
